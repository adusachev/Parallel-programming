
#include<stdio.h>
#include<iostream>
#include<stdlib.h>
#include <fstream>
#include <string>

#include <sstream>





int main(int argc, char *argv[]) {



    // int i;
    // int pid, n_proc;
    // double sum, partial_sum;
    // double h;



    // int N = 100000000;
    // // double f[N+1];
    // double* f = new double[N+1];

    // double a = 0;
    // double b = 1;
    // h = (double)b / N;


    // std::cout << "Success, empty array f length is " << N << std::endl;



    // // grid values
    // // double x[N+1];
    // double* x = new double[N+1];

    // std::cout << "Success, empty array x length is " << N << std::endl;

    // x[0] = 0;
    // for (int i = 1; i < N+1; i++) {
    //     x[i] = x[i-1] + h;
    // }


    // std::cout << "Success, full array x length is " << N << std::endl;
    

    // delete[] x;
    // delete[] f; 






    return 0;
}



